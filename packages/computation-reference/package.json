{
  "name": "computation-reference",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run src"
  },
  "dependencies": {
    "computation-ir": "workspace:*",
    "liquid-core": "workspace:*"
  },
  "devDependencies": {
    "liquid-core": "workspace:*",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
